# Copy to .env.local.

# Where apps/api listens. Must match the origin the desktop client pins, which
# is also where published quiz links point. /api/auth/* is rewritten there.
API_ORIGIN=http://localhost:3000
